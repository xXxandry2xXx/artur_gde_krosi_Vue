{
  "name": "artur_gde_krosi_vue.client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "run-p type-check \"build-only {@}\" --",
    "preview": "vite preview",
    "build-only": "vite build",
    "type-check": "vue-tsc --build --force",
    "lint": "eslint . --ext .vue,.js,.jsx,.cjs,.mjs,.ts,.tsx,.cts,.mts --fix --ignore-path .gitignore",
    "postinstall": "patch-package",
    "install-dependencies1": "node -e \"const fs = require('fs'); const pkg = require('./package.json'); const requiredLib = '@fortawesome/fontawesome-svg-core@1.x'; if (!Object.keys(pkg.dependencies).includes(requiredLib)) { console.log(`Устанавливаю ${requiredLib}...`); require('child_process').execSync(`npm install ${requiredLib}`); }\"",
    "install-dependencies2": "node -e \"const fs = require('fs'); const pkg = require('./package.json'); const requiredLib = '@fortawesome/vue-fontawesome'; if (!Object.keys(pkg.dependencies).includes(requiredLib)) { console.log(`Устанавливаю ${requiredLib}...`); require('child_process').execSync(`npm install ${requiredLib}`); }\"",
    "install-dependencies3": "node -e \"const fs = require('fs'); const pkg = require('./package.json'); const requiredLib = '@fortawesome/free-solid-svg-icons'; if (!Obj ect.keys(pkg.dependencies).includes(requiredLib)) { console.log(`Устанавливаю ${requiredLib}...`); require('child_process').execSync(`npm install ${requiredLib}`); }\"",
    "install-dependencies4": "node -e \"const fs = require('fs'); const pkg = require('./package.json'); const requiredLib = '@fortawesome/free-brands-svg-icons'; if (!Object.keys(pkg.dependencies).includes(requiredLib)) { console.log(`Устанавливаю ${requiredLib}...`); require('child_process').execSync(`npm install ${requiredLib}`); }\"",
    "install-dependencies5": "node -e \"const fs = require('fs'); const pkg = require('./package.json'); const requiredLib = 'vuex'; if (!Object.keys(pkg.dependencies).includes(requiredLib)) { console.log(`Устанавливаю ${requiredLib}...`); require('child_process').execSync(`npm install ${requiredLib}`); }\"",
    "install-dependencies6": "node -e \"const fs = require('fs'); const pkg = require('./package.json'); const requiredLib = 'vue-router'; if (!Object.keys(pkg.dependencies).in  cludes(requiredLib)) { console.log(`Устанавливаю ${requiredLib}...`); require('child_process').execSync(`npm install ${requiredLib}`); }\""
  },
  "dependencies": {
    "@fortawesome/fontawesome-svg-core": "^1.2.36",
    "@fortawesome/free-brands-svg-icons": "^6.5.2",
    "@fortawesome/free-solid-svg-icons": "^6.5.2",
    "@fortawesome/vue-fontawesome": "^3.0.8",
    "axios": "^1.6.7",
    "vue": "^3.4.15",
    "vue-router": "^4.3.3",
    "vuex": "^4.1.0"
  },
  "devDependencies": {
    "@rushstack/eslint-patch": "^1.3.3",
    "@tsconfig/node20": "^20.1.2",
    "@types/cors": "^2.8.17",
    "@types/node": "^20.11.10",
    "@vitejs/plugin-vue": "^5.0.3",
    "@vue/eslint-config-typescript": "^12.0.0",
    "@vue/tsconfig": "^0.5.1",
    "eslint": "^8.56.0",
    "eslint-plugin-vue": "^9.17.0",
    "npm-run-all2": "^6.1.1",
    "typescript": "~5.3.0",
    "vite": "^5.0.11",
    "vue-tsc": "^1.8.27"
  }
}
